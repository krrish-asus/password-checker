# password-checker
This project checks how strong a password is by measuring its entropy and identifying weak patterns. It warns the user if the password is too common, too short, or missing important character types. It helps users create safer, more secure passwords.
